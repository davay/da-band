services:

  api-dev:
    profiles: ["dev"]
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    volumes: 
      - .:/app # hot reload
    ports:
      - "8000:8000"

  api-prod:
    profiles: ["prod"]
    build:
      target: prod
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
